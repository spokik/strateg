import type { Alias, ResolvedConfig } from 'vite';
export interface AliasReplaced {
    alias: Alias;
    importeeIsRaw: boolean;
    importee: string;
    replacedImportee: string;
}
export declare class AliasContext {
    private config;
    constructor(config: ResolvedConfig);
    replaceImportee(importee: string, id: string): void | AliasReplaced;
    replaceRawImportee(rawImportee: string, id: string): void | AliasReplaced;
    private replace;
}
